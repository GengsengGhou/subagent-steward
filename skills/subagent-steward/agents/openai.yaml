interface:
  display_name: "子智能体管家"
  short_description: "Luna-first delegation with evidence-based Sol selection"
  default_prompt: "使用 $subagent-steward 完成任务：保留 Astra 主导，按需委派，优先选择合适推理强度的 Luna，有具体依据时使用 Sol。"

policy:
  allow_implicit_invocation: true
